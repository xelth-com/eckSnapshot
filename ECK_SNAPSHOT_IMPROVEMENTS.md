# Улучшения eckSnapshot: Selective .eck Directory Filtering

## 🎯 Цель

Сделать eckSnapshot умнее в обработке `.eck` директории:
- **Включать** публичные файлы (для Архитектора)
- **Исключать** конфиденциальные файлы (с секретами)
- **Универсально** - работает для любых проектов

---

## 🔍 Текущая проблема

**setup.json, строка 377:**
```json
"dirsToIgnore": [
  ".eck/"  // ← Весь .eck игнорируется!
]
```

**Последствия:**
- ❌ Архитектор НЕ видит документацию проекта
- ❌ ARCHITECTURE.md, WORKFLOWS.md и др. не попадают в snapshot
- ❌ Нет контекста о разделении ролей Архитектор/Кодер

---

## ✅ Предлагаемое решение

### Концепция: Умная фильтрация .eck

1. **По умолчанию:** Включать .eck файлы в snapshot
2. **Паттерны исключений:** Гибкая система фильтрации
3. **Проектная кастомизация:** Каждый проект может настроить свои правила

---

## 📝 Изменения в setup.json

### 1. Убрать `.eck/` из dirsToIgnore

```json
"dirsToIgnore": [
  "node_modules/",
  ".git/",
  // ".eck/",  ← УДАЛИТЬ эту строку
  "dist/",
  ...
]
```

### 2. Добавить новую секцию: eckDirectoryFiltering

```json
"fileFiltering": {
  "eckDirectoryFiltering": {
    "_comment": "Smart filtering for .eck directory - include public docs, exclude secrets",
    "enabled": true,
    "mode": "selective",
    
    "defaultBehavior": "include",
    
    "confidentialPatterns": [
      "**/SERVER_ACCESS.md",
      "**/REMOTE_DEVELOPMENT.md", 
      "**/README.md",
      "**/TROUBLESHOOTING.md",
      "**/MIGRATION_TEMPLATES.md",
      "**/SECRETS_REFERENCE.md",
      "**/credentials*.md",
      "**/secrets*.md",
      "**/*_ACCESS.md",
      "**/*_CREDENTIALS.md"
    ],
    
    "confidentialDirectories": [
      "**/snapshots/",
      "**/backups/",
      "**/private/",
      "**/confidential/"
    ],
    
    "alwaysInclude": [
      "**/ARCHITECTURE.md",
      "**/STACK.md",
      "**/WORKFLOWS.md",
      "**/DELEGATION_GUIDE.md",
      "**/OPERATIONS.md",
      "**/CONTEXT.md",
      "**/ENVIRONMENT.md",
      "**/JOURNAL.md",
      "**/ROADMAP.md",
      "**/TECH_DEBT.md"
    ],
    
    "projectOverrides": {
      "_comment": "Projects can override these patterns in their local config",
      "enabled": true,
      "configFile": ".eck/snapshot-config.json"
    }
  },
  
  "filesToIgnore": [
    ...existing...
  ],
  ...
}
```

---

## 🔧 Логика фильтрации

### Алгоритм обработки .eck директории:

```javascript
function shouldIncludeEckFile(filePath) {
  // 1. Проверить alwaysInclude (наивысший приоритет)
  if (matchesPattern(filePath, alwaysInclude)) {
    return true;
  }
  
  // 2. Проверить confidentialPatterns
  if (matchesPattern(filePath, confidentialPatterns)) {
    return false;
  }
  
  // 3. Проверить confidentialDirectories
  if (isInDirectory(filePath, confidentialDirectories)) {
    return false;
  }
  
  // 4. Применить project overrides (если есть)
  if (projectOverrides.enabled) {
    const override = checkProjectConfig(filePath);
    if (override !== null) return override;
  }
  
  // 5. Использовать defaultBehavior
  return defaultBehavior === "include";
}
```

### Приоритеты (от высшего к низшему):

1. **alwaysInclude** - всегда включать
2. **confidentialPatterns** - всегда исключать
3. **confidentialDirectories** - исключать директории
4. **projectOverrides** - проектные настройки
5. **defaultBehavior** - поведение по умолчанию

---

## 📄 Проектная кастомизация

### Файл: `.eck/snapshot-config.json` (опционально)

Проекты могут создать этот файл для своих правил:

```json
{
  "eckDirectoryFiltering": {
    "additionalConfidential": [
      "DEPLOYMENT_KEYS.md",
      "INTERNAL_DOCS.md"
    ],
    "additionalPublic": [
      "CUSTOM_GUIDE.md"
    ],
    "overrideDefaults": false
  }
}
```

**Преимущества:**
- ✅ Проект контролирует свою фильтрацию
- ✅ Не нужно менять глобальный setup.json
- ✅ .gitignore может игнорировать этот файл
- ✅ Работает для любых проектов

---

## 🎨 Примеры использования

### Пример 1: Стандартный проект

**Структура .eck:**
```
.eck/
├── ARCHITECTURE.md         ✅ Включено (alwaysInclude)
├── WORKFLOWS.md            ✅ Включено (alwaysInclude)
├── SERVER_ACCESS.md        ❌ Исключено (confidentialPatterns)
├── CUSTOM_DOC.md           ✅ Включено (defaultBehavior)
└── snapshots/              ❌ Исключено (confidentialDirectories)
    └── snapshot_1.md
```

**Результат snapshot:**
- ARCHITECTURE.md ✅
- WORKFLOWS.md ✅
- CUSTOM_DOC.md ✅
- SERVER_ACCESS.md ❌
- snapshots/ ❌

---

### Пример 2: Проект с кастомизацией

**Файл: .eck/snapshot-config.json**
```json
{
  "eckDirectoryFiltering": {
    "additionalConfidential": [
      "INTERNAL_API_KEYS.md"
    ]
  }
}
```

**Результат:**
- Все стандартные правила работают
- Плюс INTERNAL_API_KEYS.md тоже исключается

---

### Пример 3: Полностью отключить .eck

**Файл: .eck/snapshot-config.json**
```json
{
  "eckDirectoryFiltering": {
    "enabled": false
  }
}
```

**Результат:** Весь .eck игнорируется (как раньше)

---

## 🔐 Безопасность

### Паттерны по умолчанию покрывают:

**Конфиденциальные файлы:**
- `SERVER_ACCESS.md` - детали доступа к серверу
- `REMOTE_DEVELOPMENT.md` - SSH команды
- `*_ACCESS.md` - любые файлы с ACCESS в названии
- `*_CREDENTIALS.md` - любые credentials
- `credentials*.md`, `secrets*.md` - паттерны секретов

**Конфиденциальные директории:**
- `snapshots/` - старые снапшоты
- `backups/` - бэкапы
- `private/` - приватные файлы
- `confidential/` - конфиденциальные

### Что НЕ попадёт в snapshot (безопасно):

- ❌ SSH команды и хосты
- ❌ Пароли и API ключи  
- ❌ Детали подключения к БД
- ❌ Внутренние процедуры
- ❌ Старые снапшоты

### Что ПОПАДЁТ в snapshot (для Архитектора):

- ✅ Архитектура системы
- ✅ Технологический стек
- ✅ Рабочие процессы
- ✅ Руководства по делегированию
- ✅ Контекст проекта

---

## 📋 План имплементации

### Шаг 1: Обновить setup.json
```bash
cd /home/xelth-com/eckSnapshot
# Редактировать setup.json:
# 1. Убрать ".eck/" из dirsToIgnore
# 2. Добавить секцию eckDirectoryFiltering
```

### Шаг 2: Обновить код обработки файлов

**Файл для изменения:** `src/core/fileScanner.js` (или аналог)

Добавить логику:
```javascript
// Проверка .eck файлов перед фильтрацией
if (filePath.includes('.eck/')) {
  return shouldIncludeEckFile(filePath, config);
}
```

### Шаг 3: Создать утилиту фильтрации

**Новый файл:** `src/utils/eckFiltering.js`

```javascript
function shouldIncludeEckFile(filePath, config) {
  const eckConfig = config.fileFiltering.eckDirectoryFiltering;
  
  if (!eckConfig.enabled) {
    return false; // .eck отключён
  }
  
  // Применить алгоритм фильтрации...
}
```

### Шаг 4: Документация

**Обновить README.md:**
- Объяснить smart .eck filtering
- Показать примеры
- Описать проектную кастомизацию

### Шаг 5: Тестирование

```bash
# Создать snapshot проекта eckwms
cd /home/xelth-com/eckwms
eck-snapshot

# Проверить что:
# ✅ ARCHITECTURE.md включён
# ✅ WORKFLOWS.md включён  
# ❌ SERVER_ACCESS.md исключён
# ❌ snapshots/ исключёна
```

---

## 🎯 Преимущества решения

### Для всех проектов:
- ✅ Универсальное решение (не только для eckwms)
- ✅ Безопасность из коробки
- ✅ Гибкая кастомизация
- ✅ Обратная совместимость (можно отключить)

### Для Архитектора:
- ✅ Видит всю публичную документацию
- ✅ Понимает архитектуру проекта
- ✅ Знает как делегировать задачи
- ✅ НЕ видит секреты

### Для разработчиков:
- ✅ Простая настройка (опционально)
- ✅ Разделение public/private файлов
- ✅ Контроль через .eck/snapshot-config.json
- ✅ Работает "из коробки" для типичных случаев

---

## 🧪 Тестовые кейсы

### Test 1: Базовая фильтрация
```javascript
Input: .eck/ARCHITECTURE.md
Expected: Included ✅
Reason: In alwaysInclude list
```

### Test 2: Конфиденциальный файл
```javascript
Input: .eck/SERVER_ACCESS.md
Expected: Excluded ❌
Reason: Matches confidentialPatterns
```

### Test 3: Кастомный публичный файл
```javascript
Input: .eck/MY_CUSTOM_DOC.md
Expected: Included ✅
Reason: defaultBehavior = "include"
```

### Test 4: Конфиденциальная директория
```javascript
Input: .eck/snapshots/old_snapshot.md
Expected: Excluded ❌
Reason: In confidentialDirectories
```

### Test 5: Project override
```javascript
Input: .eck/INTERNAL_KEYS.md
Config: { additionalConfidential: ["INTERNAL_KEYS.md"] }
Expected: Excluded ❌
Reason: Project-specific rule
```

---

## 📊 Сравнение: До vs После

### ДО (текущее состояние):
```
Snapshot НЕ включает .eck вообще
├── ❌ ARCHITECTURE.md (пропущен)
├── ❌ WORKFLOWS.md (пропущен)
├── ❌ SERVER_ACCESS.md (пропущен - хорошо)
└── ❌ snapshots/ (пропущена - хорошо)

Результат: Архитектор слепой ❌
```

### ПОСЛЕ (с улучшениями):
```
Snapshot умно фильтрует .eck
├── ✅ ARCHITECTURE.md (включён)
├── ✅ WORKFLOWS.md (включён)
├── ❌ SERVER_ACCESS.md (исключён - безопасно)
└── ❌ snapshots/ (исключена - безопасно)

Результат: Архитектор видит контекст ✅, секреты защищены ✅
```

---

## 🚀 Следующие шаги

1. **Утвердить концепцию** с пользователем
2. **Обновить setup.json** с новой секцией
3. **Реализовать логику фильтрации** в коде
4. **Протестировать на eckwms**
5. **Обновить документацию**
6. **Опубликовать новую версию**

---

**Статус:** 📋 Proposal готов  
**Следующий шаг:** Получить одобрение и начать имплементацию
